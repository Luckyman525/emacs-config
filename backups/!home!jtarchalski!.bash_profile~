#
# ~/.bash_profile
#

PS1="[\W] -> "

export EDITOR="emacs -nw"

export PATH=$PATH:/home/jtarchalski/.gem/ruby/2.6.0/bin

export LPASS_CLIPBOARD_COMMAND="xclip -selection clipboard -in -l 1"

#export LPASS_CLIPBOARD_COMMAND="xsel -t 5000 --input --clipboard"

#aur sync -cguT linux-rt-lts linux-rt-lts-headers

[[ -f ~/.bashrc ]] && . ~/.bashrc
export GPG_TTY=$(tty)
